{
  "name": "redirect",
  "version": "1.0.0",
  "description": "This repository only contains my redirect links",
  "private": false,
  "homepage": "/redirect",
  "license": "MIT",
  "dependencies": {
    "react": "16.9.0",
    "react-dom": "16.9.0",
    "react-router-dom": "5.2.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "deploy": "push-dir --dir=build --branch=gh-pages",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "postbuild": "react-snapshot"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "push-dir": "0.4.1",
    "react-scripts": "3.1.1",
    "react-snapshot": "^1.3.0"
  }
}
